# CPU version for inference web service
# Core dependencies
fastapi==0.115.4
uvicorn==0.32.0
pydantic==2.9.2

# PyTorch (CPU version, 如果需要GPU版本请使用conda安装)
torch==2.5.1
--extra-index-url https://download.pytorch.org/whl/cpu

# Audio processing
soundfile==0.12.1
scipy==1.14.1
numpy==2.0.2
torchaudio

# Utils
pytz==2024.2
PyYAML==6.0.2
python-multipart==0.0.17  # 用于处理文件上传

# Optional but recommended
python-dotenv==1.0.1  # 环境变量管理